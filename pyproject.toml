[project]
name = "sunbiz"
version = "0.1.0"
description = "Scraper Sunbiz corporation search"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "camoufox",
    "rich",
    "python-telegram-bot>=21.0",
]

[tool.uv]
dev-dependencies = []
